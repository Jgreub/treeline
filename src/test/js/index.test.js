beforeEach(module('treeline'));

describe('project controller', function() {

//    beforeEach(inject(function($controller, $http) {
//        jasmine.spyOn($http.get).and.returnValue()
//        this.ctrl = $controller('ProjectController', { $http: });
//    }));

    it('should have a the number of total items', function() {
        expect(5).toBeDefined();
    });
});